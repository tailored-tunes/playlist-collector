{
  "name": "playlist-collector",
  "version": "0.0.1",
  "description": "Tailored Tunes playlist collector",
  "main": "web.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
    "engines": {
    "node": "0.10.x"
  },
  "dependencies": {
    "express" : "*",
    "logfmt" : "*"
  }
}
